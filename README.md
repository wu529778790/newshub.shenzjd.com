# 🔥 NewsHub - 热点内容聚合器

一个高性能的热点内容聚合器，从多个平台（微博、知乎、百度、Bilibili 等）抓取热门数据，提供统一的浏览界面。

## ✨ 核心特性

- **多平台聚合**：支持 27+ 个数据源
- **拖拽排序**：自定义数据源显示顺序
- **图片分享**：一键生成精美分享图
- **智能缓存**：内存缓存 + 自动过期
- **响应式设计**：完美适配移动端
- **深色模式**：支持主题切换

## 🚀 快速开始

### 本地开发

```bash
pnpm install
pnpm dev
# 访问 http://localhost:3000
```

### Docker 部署

```bash
# Docker Hub
docker run -p 3000:3000 wu529778790/newshub.shenzjd.com:latest

# GitHub Container Registry
docker run -p 3000:3000 ghcr.io/wu529778790/newshub.shenzjd.com:latest
```

### Docker Compose

```yaml
version: '3.8'
services:
  newshub:
    image: wu529778790/newshub.shenzjd.com:latest
    ports:
      - "3000:3000"
    restart: unless-stopped
```

```bash
docker-compose up -d
```

## 🐳 构建与部署

### 自动构建

GitHub Actions 会在以下情况自动构建 Docker 镜像：
- 推送到 `main` 分支 → `latest` 标签
- 创建版本标签 → 版本号标签

镜像同时推送到：
- Docker Hub: `wu529778790/newshub.shenzjd.com`
- GHCR: `ghcr.io/wu529778790/newshub.shenzjd.com`

### 手动构建

```bash
# 构建镜像
docker build -t newshub .

# 运行容器
docker run -p 3000:3000 newshub
```

## 📊 数据源列表

| 类型 | 平台 | 刷新频率 |
|------|------|----------|
| 热点 | 微博、知乎、百度、B站、抖音、虎扑、贴吧、今日头条 | 2-10 分钟 |
| 实时 | IT之家、金十数据、36氪、格隆汇、雪球 | 2-5 分钟 |
| 新闻 | 澎湃新闻、参考消息、卫星通讯社、凤凰网 | 30 分钟 |
| 科技 | Solidot、GitHub、少数派、稀土掘金 | 30-60 分钟 |

## 🔧 常用命令

```bash
pnpm dev          # 开发模式
pnpm build        # 构建生产版本
pnpm preview      # 预览生产构建
pnpm type-check   # TypeScript 检查
pnpm test         # 运行测试
```

## 📄 许可证

MIT
